package com.example.controller;import com.example.entity.Doctor;import com.example.service.DoctorService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.web.bind.annotation.PathVariable;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RestController;/** * @Auther: Zhou_Li * @Date: 2021/7/8 10:57 * @Description: */@RestController@RequestMapping("/testBoot")public class DoctorController {    @Autowired    private DoctorService doctorService;    // request format    @RequestMapping("getDoctor/{id}")    public String GetDoctor(@PathVariable int id){        return doctorService.selById(id).toString();    }    @RequestMapping("selDoctor/{name}")    public String SelDoctor(@PathVariable String name){        Doctor[] doctors = doctorService.selByName(name);        String str = "";        int len = doctors.length;        for(int i = 0 ; i < len ; i++)            str += doctors[i].toString() + ";";        return str;    }    @RequestMapping("getAll")    public String GetAllDoctor(){        Doctor[] doctors = doctorService.findAllDoctor();        String str = "";        int len = doctors.length;        for(int i = 0 ; i < len ; i++)            str += doctors[i].toString() + ";";        return str;    }}